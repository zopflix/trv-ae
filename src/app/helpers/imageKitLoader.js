export const trvLoader = ({ src }) => {
  return `https://assets.travanya.com/${src}`
}

export const airlineLogoLoader = ({ src }) => {
  return `https://assets.shipratravel.com/${src}`
}

export const sfLoader = ({ src }) => {
  return src
}